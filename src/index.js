// flightlog — a flight recorder for your app.
// 0.0.1 is a name-reservation placeholder. The real implementation lands in 0.1.0.
// Planned API: install(opts?) -> { capture }.  See README.md and docs/PRD.

throw new Error(
  'flightlog@0.0.1 is a placeholder reserving the package name. ' +
  'The implementation is not released yet — see https://github.com/hamr0/flightlog'
);
