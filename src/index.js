// flightlog — a flight recorder for your app.
// The whole public surface is install(); see README.md + flightlog.context.md.
export { install } from './install.js';

/** @typedef {import('./types.js').InstallOptions} InstallOptions */
/** @typedef {import('./types.js').LogRecord} LogRecord */
