# PRD — flightlog (local error catcher)

**Date:** 2026-05-31
**Status:** PRD — decisions locked, name reserved on npm. **Implementation complete (M1–M4 built + tested, 30 green incl. a packed-tarball E2E; `tsc` clean; `0.1.0` packaged); security-audited (log file created `0600`).** npm Trusted Publisher configured (2026-05-31). Only remaining step: dispatch `publish.yml` to ship `0.1.0`.
**npm name:** `flightlog` (reserved — `0.0.1` placeholder published). Tagline: *a flight recorder for your app.*
**License:** Apache-2.0.
**Supersedes:** `docs/plans/2026-05-31-barecatch-design.md` (folded into this PRD).

---

## 1. Problem

Errors in the apps I run (addypin, gitdone, latefyi backends) either crash a
process silently or fail in the dark — I find out late or never. Each app handles
errors differently or not at all. Sentry solves this but is the wrong shape and
wrong time for a solo, pre-scale, zero-telemetry builder: its one hard pillar
(aggregation at volume) doesn't matter until I have more errors than I can read
by hand, and its context-capture is the surveillance payload my privacy work
exists to refuse. What I actually need is the ~20-line global error net,
packaged once so it wires into every app the same way.

## 2. Goal

A zero-dependency Node library that catches the errors my apps would otherwise
lose and appends each as one JSON line to a local JSONL file I can read at any
time — even on a healthy app — to see *where things have failed*.

- **Nothing disappears** — every error lands somewhere findable.
- **Jumpstart to the failure** — a predictable local JSONL I reach into anytime.
- **One net, everywhere** — identical, audited handling across all my apps.
- **Silent + lightweight** — never becomes the bug, never phones home, zero deps.

## 3. Success criteria (how we know it worked)

1. Wiring an app to it is ≤ 3 lines and adds **zero** production dependencies.
2. All four capture cases land one correct JSONL line: uncaught, unhandled
   rejection, manual `capture()`, and non-Error thrown values.
3. A long-running server cannot fill its disk via the log (bounded by rotation).
4. A broken sink (perms/disk/readonly) never crashes the app **and** is never
   silent (one stderr warning).
5. The file is readable by `tail`/`jq`/editor with no special tooling.
6. No context is ever captured that the adopter did not explicitly pass.

## 4. What it is NOT (the refusals — these *are* the product)

- **No aggregation / dedup / counts** — scale-gated; `jq` covers it when needed.
- **No breadcrumbs / auto-captured context** — the surveillance payload a privacy
  suite refuses. **Default-out on context, always.**
- **No UI, no server, no reader** — the JSONL *is* the interface. A thin CLI
  reader may bolt on later iff BYO-`jq` proves annoying; format won't change.
- **No symbolication / alerting / release tracking** — team/scale process.
- **Not a general logger** — errors only; no info/warn levels (pino/winston
  territory). Possibly revisited, not on spec.
- **No restart logic** — crash-loop backoff is the supervisor's job.

## 5. Decisions (locked)

| # | Decision | Rationale |
|---|----------|-----------|
| Target | **Node server first.** Browser = separate entry/lib *iff* a real use case appears; `wearehere` stays out (privacy contradiction). | No contradiction server-side; browser surface thin + constrained. |
| Read surface | **Plain JSONL, BYO reader.** No reader ships. | Universal, pluggable, zero surface to secure. |
| Scope | **Errors only** — uncaught, unhandledRejection, manual `capture()`. | Sharp, honest, stays bare. |
| Crash policy | **Default crash-and-restart** on uncaught (`exit(1)`); `exitOnUncaught` knob; **rejections log-only**. | Twelve-factor disposability + Node guidance; knob because not every consumer is a supervised server. |
| File growth | **Built-in size cap + rotate** default on; `maxBytes:0` disables. Rename to `.1` at threshold; keep current + one previous (~2×`maxBytes`). | Caps disk forever, zero config, works in containers/Windows/VPS. No compression/date/retention (fluff). |
| Self-failure | **Swallow + warn-once-to-stderr** (reset-on-success) + **boot-time writability check**. | Logger must never crash the app; broken sink must be loud, once, on a channel that can't itself fail. Fail at boot, not in the dark. |
| `capture()` | `capture(err, extraContext?)`; context **default-out**. | Mechanism in lib, policy in adopter. |
| Write mode | **async (`appendFile`) on normal path; sync only in uncaught→exit path** so the death line flushes before `exit(1)`. | Don't freeze the server per error; guarantee the final line lands. |
| Consumption | `npm i <name>` → `import { install }` → 3-line wire-up. **ESM-only**, JS + JSDoc→`.d.ts`. | LIBRARY_CONVENTIONS. |
| Dependencies | **Zero prod deps.** Vanilla + `node:fs`. `typescript`/`@types/node` dev-only. | AGENT_RULES dependency hierarchy, tiers 1–2. |

## 6. Public API (whole surface)

```js
import { install } from 'flightlog';

const { capture } = install({
  file: '/var/log/addypin/errors.jsonl', // sink; omit → stderr
  context: { app: 'addypin', release: 'v1.4.2' }, // static, adopter-chosen
  exitOnUncaught: true,   // default; false = log-and-stay-alive (CLI/desktop)
  maxBytes: 5_000_000,    // confirmed default: 5 MB; 0 = disable rotation
});

try { risky(); } catch (err) { capture(err, { where: 'checkout', userId }); }
```

- `install(opts?) → { capture }` — registers global handlers, runs boot-time
  writability check, returns `capture`.
- `capture(err, extra?)` — normalize any thrown value, append one line with
  `{ ...context, ...extra }`.

**Record shape:**
```json
{"ts":"...","kind":"uncaught","name":"TypeError","message":"...","stack":"...","app":"addypin","release":"v1.4.2"}
```
`kind` ∈ `uncaught | unhandledRejection | manual`. Everything after `stack` is
adopter-supplied context only.

## 7. Error-class model (resolves "keep alive vs fail")

1. **Operational errors** (handler throws, DB fails, bad input) — *expected*.
   Caught at the boundary via `capture()`; that one request fails; **server stays
   up**. This is "keep alive on small forgivable errors."
2. **Uncaught exceptions** — escaped every handler; event loop in unknown state.
   Log (sync) → `exit(1)` → supervisor restarts clean. Keeps firing → supervisor
   backoff gives up after N, stays **down loudly**. This is "major failure →
   better to fail."

## 8. Self-failure handling

Write wrapped in try/catch (never propagates). First failure → one stderr line
naming the errno; `warned` flag prevents spam; reset on next success; stderr
write itself wrapped (failsafe). Likely errnos: `EACCES` (perms/chmod), `EROFS`
(read-only container fs), `ENOENT` (missing dir), `ENOSPC` (disk full), `EDQUOT`
(quota). Boot check (`mkdirSync` recursive + probe append) surfaces a misconfigured
path at startup, not at the first real error.

## 9. Build / ship plan (per LIBRARY_CONVENTIONS)

### 9.1 Source shape
- `src/index.js` — Node entry, **pure ESM + JSDoc, no build step for shipped code**
  (the `.js` authored is the `.js` shipped).
- `src/types.js` — shared `@typedef`s (`InstallOptions`, `LogRecord`) as JSDoc, so
  cross-file shapes generate their own `.d.ts` (no hand-written `.d.ts`).
- Zero production dependencies (vanilla + `node:fs`); the one-prod-dep *ceiling* is
  untouched. `typescript` + `@types/node` are devDeps only.

### 9.2 `package.json` contract
- `"type": "module"`, `main: ./src/index.js`.
- `exports."."` with **`types` first**, then `default` → `./src/index.js`.
- `files`: `src/`, `types/`, `README.md`, `flightlog.context.md`, `CHANGELOG.md`
  (ship these; **never** ship `docs/`, `CLAUDE.md`, `.github/`, `poc/`).
- `engines.node`: `>=18` (floor; CI runs 22).
- Scripts: `"typecheck": "tsc --noEmit"`, `"build:types": "tsc"`,
  `"prepublishOnly": "npm run build:types"`, `"test": "node --test"` (stdlib test
  runner — no test-framework dep).
- **No `bin`** (no CLI in scope). **No `.env`/`.env.example`** — flightlog takes
  config via `install()` args, not the environment; the 12-factor "config in env"
  essential is N/A for an in-process library and is deliberately skipped.

### 9.3 Types — anti-drift contract (the five invariants)
- JSDoc is the **only** hand-authored source of types; no hand-edited `.d.ts`.
- `.d.ts` generated by `tsc`, **git-ignored** (`/types/`), built on publish via
  `prepublishOnly` → can't go stale.
- `tsconfig.json`: `allowJs`+`checkJs`, `emitDeclarationOnly`, `declarationDir
  ./types`, `module/moduleResolution nodenext`, `strict: false` + **`strictNullChecks: true`** (keep the 5% null-safety, drop the `noImplicitAny` noise).
- CI runs `tsc --noEmit` on every push/PR **and** before publish — a JSDoc/code
  mismatch blocks merge and blocks publish.
- Fix type findings with minimal guards only — **never** `!`, `as any`, `@ts-ignore`.

### 9.4 Doc set
| File | Ships? | Purpose |
|---|---|---|
| `README.md` | yes | pitch + quickstart + positioning (the hook); follows the bare-suite README *structure* (logo/tagline/badges → What this is → Install → Quick start → record/options → Docs → where-it-sits → License) while §10 keeps flightlog **outside** the bare-suite badge |
| `flightlog.context.md` | yes | complete adopter contract: all options, full API, §4 refusals, §9.6 threat model, gotchas |
| `CHANGELOG.md` | yes | keep-a-changelog; an entry every release |
| `CLAUDE.md` | **no** (repo-only) | agent doctrine + most-litigated refusals + where rationale lives (this PRD) |
| `docs/` (this PRD, future SPEC/TASKS) | **no** (repo-only) | product/impl decisions |

### 9.5 Tests (Testing Trophy — mostly integration, `node --test`)
- Integration: `tmp_path` file, assert one correct JSONL line per `kind`
  (uncaught / unhandledRejection / manual); rotation at `maxBytes` (renames to
  `.1`, caps disk); self-failure swallow + warn-once (reset-on-success);
  `exitOnUncaught` both ways; boot-time writability check fails loud on a bad path.
- Unit: `normalize()` on non-Error throws and missing-stack values.

### 9.6 Threat model (for context.md)
flightlog never harvests context — but the JSONL **will** contain whatever the
adopter passes to `install({context})` / `capture(err, extra)`, plus error
messages/stacks that may incidentally include sensitive strings. Therefore:
adopters own what they put in context; the log file inherits the sensitivity of
its contents (keep it off shared/world-readable paths). This is stated up front so
"local + private" isn't misread as "safe to pass secrets to." **Hardening (added
post-audit):** flightlog creates the log file `0600` (owner-only) by default, so
it isn't group/world-readable on a shared host; the mode applies only at creation.

### 9.7 CI / release
- `ci.yml` (push/PR): `npm ci` → `typecheck` → `build:types` → `test`. **No lint
  step** (`tsc` covers the bug class; ESLint ecosystem cuts against the dep
  hierarchy). — *still to add.*
- `publish.yml` (manual `workflow_dispatch`, OIDC trusted publishing, idempotent,
  asserts end-state): **added** (`mailproof` canonical workflow, verbatim).
- **One-time before first OIDC publish:** configure the Trusted Publisher at
  npmjs.com (Package → Settings → Trusted Publisher → GitHub Actions: repo +
  `publish.yml`). Until then the only published artifact is the `0.0.1`
  token/login placeholder.

### 9.8 Version strategy
- `0.0.1` = name reservation (placeholder, throws). `0.1.0` = first real release.
- SemVer. **Post-1.0 scope freeze:** after 1.0 only security fixes, API-preserving
  bug fixes, doc corrections, and pulling-existing-mechanism-into-a-helper ship.
- Repo: `github.com/hamr0/flightlog`.

### 9.9 Build order — modular, per AGENT_RULES

"Build incrementally; small independent modules; **each piece must work on its own
before integrating the next.**" Not over-split — flightlog is small. A module is
"done" only when *its own* tests pass; only then move on.

**M1 — `normalize(value, kind, context)` → record.** ✅ **Built + tested** (`src/normalize.js`, 11 unit tests). Pure, zero I/O. Any thrown
value (Error or not) → flat JSON-safe record (`ts, kind, name, message, stack,
…context`). Context is spread in but never harvested. *Fixes the POC finding:
capture the stack at the call boundary for non-Error throws.*
- **Done when (unit):** Error, non-Error (string/object/null), and missing-stack
  inputs all yield correct records; context merge order is right; no field appears
  that the caller didn't supply. → satisfies success criteria §3.2, §3.6.

**M2 — `sink({ file, maxBytes })` → `{ write, writeSync }`.** ✅ **Built + tested** (`src/sink.js`, 11 integration tests; `maxBytes` default `5_000_000`; log file created `0600`). All file I/O, no
global handlers. Async `write` (normal path), `writeSync` (death path); rotation
at `maxBytes` (rename → `.1`, keep current + one previous); self-failure swallow +
warn-once-to-stderr (reset-on-success); boot-time writability check (`mkdirSync`
recursive + probe append).
- **Done when (integration, `tmp` file):** appends one JSONL line per call;
  rotates at `maxBytes`; a bad path fails loud at boot; a simulated write failure
  is swallowed (no throw) and emits exactly one stderr warning. → §3.1, §3.3,
  §3.4, §3.5.

**M3 — `install(opts)` → `{ capture }`.** ✅ **Built + tested** (`src/install.js`, 7 subprocess integration tests). Rejection-log-only gate **confirmed** (2026-05-31). Wires M1 + M2: registers
`uncaughtException` (writeSync → `exit(1)` unless `exitOnUncaught: false`) and
`unhandledRejection` (write, log-only — *note it suppresses Node's default crash*);
returns `capture(err, extra)` = `normalize(…, 'manual')` → write.
- **Done when (integration):** each `kind` produces its line; `exitOnUncaught`
  both ways behaves; `capture()` round-trips. → §3.2 (full), crash policy §7.
- **Gate:** ✅ unhandled-rejection log-only confirmed (2026-05-31) — documented as a gotcha in `flightlog.context.md`.

**M4 — integrate + package.** ✅ **Done** (`tsc --noEmit` clean; 30 tests green incl. a packed-tarball E2E that imports `flightlog` by bare specifier; `npm pack` ships exactly the `files` allowlist; `0.0.1` placeholder throw replaced; version bumped `0.1.0`). `src/types.js` (`@typedef`s); `tsconfig` + devDeps +
scripts; `ci.yml`; `flightlog.context.md`; CHANGELOG entry; replace the `0.0.1`
placeholder throw; bump `0.1.0`.
- **Done when:** `tsc --noEmit` clean, full suite green, `npm pack --dry-run`
  ships exactly the `files` allowlist.

Each module is its own commit (or small PR) with its tests green before the next.

## 10. Naming decision (RESOLVED → `flightlog`)

This is **Substrate**, not the bare suite — the bare suite is "zero-deps **agent**
infrastructure" (gives AI agents capabilities); this gives **my apps** error
capture. Closest sibling: `mailproof`.

**Chosen: `flightlog`** — flight-recorder metaphor (survives the crash, readable
anytime); carries both halves of the goal; no false agent-suite membership.
Rejected: `barecatch` (borrows the agent-suite badge), `errkeep` (less evocative),
`lastwords` (undersells the always-on `capture()` path).

## 11. Go / No-Go gate

**GO to write `src/` when:**
- [x] Name chosen (§10): `flightlog`.
- [x] Name reserved on npm (`0.0.1` placeholder; relicensed Apache-2.0 in-repo for `0.1.0`+).
- [x] This PRD committed to git (grounds the build).
- [x] `publish.yml` added (§9.7).
- [ ] §3 success criteria accepted as the definition of done.

**Before first real publish (`0.1.0`):**
- [x] `ci.yml` added (§9.7).
- [x] `tsconfig.json` + devDeps + `package.json` scripts wired (§9.2–9.3).
- [x] `flightlog.context.md` + `CHANGELOG.md` written (§9.4).
- [x] Trusted Publisher configured at npmjs.com (§9.7) — confirmed configured (2026-05-31).
- [x] Repo pushed to `github.com/hamr0/flightlog`.

**NO-GO / revisit triggers (stop and re-open the PRD if any becomes true):**
- A second production dependency looks necessary → reconsider scope.
- Aggregation/dedup feels needed → you've hit the scale wall; re-evaluate vs a
  self-hosted sink, don't bolt it on here.
- A browser need appears → new PRD for a separate entry point, don't widen this.

## 12. Open / deferred

- Browser entry point — deferred until a concrete need.
- CLI reader — deferred; format-compatible if added.
- Levels logger — out of scope, possibly revisited.

## 13. Provenance

POC at `poc/` validated all four capture cases in 47 zero-dep lines; throwaway
per AGENT_RULES (POC-first). One finding folded in: capture the stack at the call
boundary for non-Error throws (POC pointed it into `normalize()`).
