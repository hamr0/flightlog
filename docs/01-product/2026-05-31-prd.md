# PRD — flightlog (local error catcher)

**Date:** 2026-05-31
**Status:** PRD — decisions locked, name reserved on npm, pre-implementation
**npm name:** `flightlog` (reserved — `0.0.1` placeholder published). Tagline: *a flight recorder for your app.*
**License:** Apache-2.0.
**Supersedes:** `docs/plans/2026-05-31-barecatch-design.md` (folded into this PRD).

---

## 1. Problem

Errors in the apps I run (addypin, gitdone, latefyi backends) either crash a
process silently or fail in the dark — I find out late or never. Each app handles
errors differently or not at all. Sentry solves this but is the wrong shape and
wrong time for a solo, pre-scale, zero-telemetry builder: its one hard pillar
(aggregation at volume) doesn't matter until I have more errors than I can read
by hand, and its context-capture is the surveillance payload my privacy work
exists to refuse. What I actually need is the ~20-line global error net,
packaged once so it wires into every app the same way.

## 2. Goal

A zero-dependency Node library that catches the errors my apps would otherwise
lose and appends each as one JSON line to a local JSONL file I can read at any
time — even on a healthy app — to see *where things have failed*.

- **Nothing disappears** — every error lands somewhere findable.
- **Jumpstart to the failure** — a predictable local JSONL I reach into anytime.
- **One net, everywhere** — identical, audited handling across all my apps.
- **Silent + lightweight** — never becomes the bug, never phones home, zero deps.

## 3. Success criteria (how we know it worked)

1. Wiring an app to it is ≤ 3 lines and adds **zero** production dependencies.
2. All four capture cases land one correct JSONL line: uncaught, unhandled
   rejection, manual `capture()`, and non-Error thrown values.
3. A long-running server cannot fill its disk via the log (bounded by rotation).
4. A broken sink (perms/disk/readonly) never crashes the app **and** is never
   silent (one stderr warning).
5. The file is readable by `tail`/`jq`/editor with no special tooling.
6. No context is ever captured that the adopter did not explicitly pass.

## 4. What it is NOT (the refusals — these *are* the product)

- **No aggregation / dedup / counts** — scale-gated; `jq` covers it when needed.
- **No breadcrumbs / auto-captured context** — the surveillance payload a privacy
  suite refuses. **Default-out on context, always.**
- **No UI, no server, no reader** — the JSONL *is* the interface. A thin CLI
  reader may bolt on later iff BYO-`jq` proves annoying; format won't change.
- **No symbolication / alerting / release tracking** — team/scale process.
- **Not a general logger** — errors only; no info/warn levels (pino/winston
  territory). Possibly revisited, not on spec.
- **No restart logic** — crash-loop backoff is the supervisor's job.

## 5. Decisions (locked)

| # | Decision | Rationale |
|---|----------|-----------|
| Target | **Node server first.** Browser = separate entry/lib *iff* a real use case appears; `wearehere` stays out (privacy contradiction). | No contradiction server-side; browser surface thin + constrained. |
| Read surface | **Plain JSONL, BYO reader.** No reader ships. | Universal, pluggable, zero surface to secure. |
| Scope | **Errors only** — uncaught, unhandledRejection, manual `capture()`. | Sharp, honest, stays bare. |
| Crash policy | **Default crash-and-restart** on uncaught (`exit(1)`); `exitOnUncaught` knob; **rejections log-only**. | Twelve-factor disposability + Node guidance; knob because not every consumer is a supervised server. |
| File growth | **Built-in size cap + rotate** default on; `maxBytes:0` disables. Rename to `.1` at threshold; keep current + one previous (~2×`maxBytes`). | Caps disk forever, zero config, works in containers/Windows/VPS. No compression/date/retention (fluff). |
| Self-failure | **Swallow + warn-once-to-stderr** (reset-on-success) + **boot-time writability check**. | Logger must never crash the app; broken sink must be loud, once, on a channel that can't itself fail. Fail at boot, not in the dark. |
| `capture()` | `capture(err, extraContext?)`; context **default-out**. | Mechanism in lib, policy in adopter. |
| Write mode | **async (`appendFile`) on normal path; sync only in uncaught→exit path** so the death line flushes before `exit(1)`. | Don't freeze the server per error; guarantee the final line lands. |
| Consumption | `npm i <name>` → `import { install }` → 3-line wire-up. **ESM-only**, JS + JSDoc→`.d.ts`. | LIBRARY_CONVENTIONS. |
| Dependencies | **Zero prod deps.** Vanilla + `node:fs`. `typescript`/`@types/node` dev-only. | AGENT_RULES dependency hierarchy, tiers 1–2. |

## 6. Public API (whole surface)

```js
import { install } from 'flightlog';

const { capture } = install({
  file: '/var/log/addypin/errors.jsonl', // sink; omit → stderr
  context: { app: 'addypin', release: 'v1.4.2' }, // static, adopter-chosen
  exitOnUncaught: true,   // default; false = log-and-stay-alive (CLI/desktop)
  maxBytes: 5_000_000,    // default ~5MB; 0 = disable rotation
});

try { risky(); } catch (err) { capture(err, { where: 'checkout', userId }); }
```

- `install(opts?) → { capture }` — registers global handlers, runs boot-time
  writability check, returns `capture`.
- `capture(err, extra?)` — normalize any thrown value, append one line with
  `{ ...context, ...extra }`.

**Record shape:**
```json
{"ts":"...","kind":"uncaught","name":"TypeError","message":"...","stack":"...","app":"addypin","release":"v1.4.2"}
```
`kind` ∈ `uncaught | unhandledRejection | manual`. Everything after `stack` is
adopter-supplied context only.

## 7. Error-class model (resolves "keep alive vs fail")

1. **Operational errors** (handler throws, DB fails, bad input) — *expected*.
   Caught at the boundary via `capture()`; that one request fails; **server stays
   up**. This is "keep alive on small forgivable errors."
2. **Uncaught exceptions** — escaped every handler; event loop in unknown state.
   Log (sync) → `exit(1)` → supervisor restarts clean. Keeps firing → supervisor
   backoff gives up after N, stays **down loudly**. This is "major failure →
   better to fail."

## 8. Self-failure handling

Write wrapped in try/catch (never propagates). First failure → one stderr line
naming the errno; `warned` flag prevents spam; reset on next success; stderr
write itself wrapped (failsafe). Likely errnos: `EACCES` (perms/chmod), `EROFS`
(read-only container fs), `ENOENT` (missing dir), `ENOSPC` (disk full), `EDQUOT`
(quota). Boot check (`mkdirSync` recursive + probe append) surfaces a misconfigured
path at startup, not at the first real error.

## 9. Build / ship plan (per LIBRARY_CONVENTIONS)

- `src/index.js` — Node entry, pure ESM + JSDoc.
- Types: JSDoc → generated `.d.ts` via `tsc` (git-ignored, built on publish); CI
  `tsc --noEmit` (`checkJs` + `strictNullChecks`).
- `flightlog.context.md` — adopter contract incl. the §4 refusals.
- Tests (Testing Trophy, mostly integration): `tmp_path` file, assert JSONL per
  `kind`; rotation at `maxBytes`; self-failure swallow + warn-once;
  `exitOnUncaught` both ways. Few unit tests for `normalize()`.
- CI: `ci.yml` (typecheck → build:types → test); `publish.yml` (OIDC, manual).
- New repo: `github.com/hamr0/flightlog`.

## 10. Naming decision (RESOLVED → `flightlog`)

This is **Substrate**, not the bare suite — the bare suite is "zero-deps **agent**
infrastructure" (gives AI agents capabilities); this gives **my apps** error
capture. Closest sibling: `mailproof`.

**Chosen: `flightlog`** — flight-recorder metaphor (survives the crash, readable
anytime); carries both halves of the goal; no false agent-suite membership.
Rejected: `barecatch` (borrows the agent-suite badge), `errkeep` (less evocative),
`lastwords` (undersells the always-on `capture()` path).

## 11. Go / No-Go gate

**GO when, before writing `src/`:**
- [x] Name chosen (§10): `flightlog`.
- [x] Name reserved on npm (`0.0.1` placeholder published; relicensed Apache-2.0 in-repo for `0.1.0`+).
- [x] This PRD committed to git (grounds the build).
- [ ] Repo pushed to `github.com/hamr0/flightlog`.
- [ ] §3 success criteria accepted as the definition of done.

**NO-GO / revisit triggers (stop and re-open the PRD if any becomes true):**
- A second production dependency looks necessary → reconsider scope.
- Aggregation/dedup feels needed → you've hit the scale wall; re-evaluate vs a
  self-hosted sink, don't bolt it on here.
- A browser need appears → new PRD for a separate entry point, don't widen this.

## 12. Open / deferred

- Browser entry point — deferred until a concrete need.
- CLI reader — deferred; format-compatible if added.
- Levels logger — out of scope, possibly revisited.

## 13. Provenance

POC at `poc/` validated all four capture cases in 47 zero-dep lines; throwaway
per AGENT_RULES (POC-first). One finding folded in: capture the stack at the call
boundary for non-Error throws (POC pointed it into `normalize()`).
